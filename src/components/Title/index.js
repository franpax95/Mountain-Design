import React from 'react';
import './styles.css';

export const ServiceTitle = ({ children }) => <div className="ServiceTitle">{children}</div>;
const Title = ({ children }) => <div className="Title">{children}</div>;

export default Title;